#pragma once

// ============================================================================
// io.h
// ----------------------------------------------------------------------------
// Загрузка сценария из текстового файла.
// Поддерживается формат Stage 4 / Stage 6 с блоком GRID.
// ============================================================================

#include "types.h"
#include <string>

bool loadScenario(
    const char* filename,
    int& N,
    int& M,
    int field[MAX_N][MAX_M],
    Pos& start,
    Pos exits[MAX_EXITS],
    int& exitCount
);

// Возвращает удобный для пользователя путь: если расширение .txt не указано,
// функция автоматически добавит его. При необходимости может вернуть и
// исходную строку без изменений.
std::string resolveScenarioFilename(const std::string& userInput);
